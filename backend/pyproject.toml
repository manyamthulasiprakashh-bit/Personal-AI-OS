[project]
name = "personal-ai-os-backend"
version = "0.1.0"
description = "Watari backend"
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
src = ["app"]

[tool.black]
line-length = 100
target-version = ['py312']
